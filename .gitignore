.packwiz/
*.log
*.mrpack
*.bak
.claude/*
CLAUDE.md
.dev
docs/*
# All dev-only artifacts live under .dev/ — not committed, not shipped.
# Includes: test-configs (Prism), live-configs (server backup), docs, old-releases.
.dev/
